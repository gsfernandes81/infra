---
# The container runtime, and nothing else. If something new lands here it needs the same
# kind of justification this has: the hosts genuinely cannot share it.
extra_packages:
  - docker
  - docker-cli-compose
