---
# Hardware- and role-driven, not taste. Everything here is already installed; the list
# exists so a rebuilt `zero` gets it without anyone recalling why.
extra_packages:
  - bcache-tools     # the data array assembles from bcache devices — see recovery.md
  - btrfs-progs
  - smartmontools    # present, verified 2026-08-21. NEVER pair with smartd — see CLAUDE.md
  - docs
