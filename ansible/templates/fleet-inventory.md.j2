# Fleet inventory

<!--
  GENERATED by ansible/playbooks/audit.yml. Do not edit by hand — the next run overwrites
  it, and a hand-edit here is exactly the drift this file exists to end.

  It records what the BOXES say, not what this repo says, and that is the point: every
  fact below used to live in prose in three places and had drifted in all three. Anything
  here that surprises you is a real difference between the repo and the fleet.

  THIS FILE IS COMMITTED. Whatever the audit collects lands in git history. That is why
  the playbook extracts eight named container fields rather than dumping inspect output:
  Config.Env on the bot containers holds live tokens. If you widen what is gathered,
  widen it knowing this file is the destination.

  Commit FULL runs only. A --limit run says PARTIAL RUN at the top and must not be
  committed over a complete one.
-->

Generated {{ report_generated }} — from the hosts, not from this repo.

Asked this run: {{ audited_hosts | join(', ') }}.
{% if audited_hosts | length < groups['fleet'] | length %}
**PARTIAL RUN.** Hosts missing from that list were **not asked** — they are rendered
below as UNREACHABLE, which here means "not audited", not "down". Do not read this file
as a picture of the fleet.
{% endif %}

{% for host in groups['fleet'] %}
{% set h = hostvars[host] %}
{% set facts = h.ansible_facts | default({}) %}
## `{{ host }}`

{% if facts.distribution is defined %}
{{ facts.distribution }} {{ facts.distribution_version }} · kernel {{ facts.kernel }}
<!-- No arch or RAM here: hosts/<host>/system/hardware.md owns what does not change,
     this file owns what does. Reporting both in two formats is what started this. -->
{% else %}
**UNREACHABLE** — no facts gathered. Nothing below is known for this host.
{% endif %}

### Containers

{% if host not in groups['containerized'] | default([]) %}
Not audited for containers by design — see `ansible/inventory`.
{% elif h.audit_containers is not defined or h.audit_containers.rc | default(1) != 0 %}
**UNREADABLE.** The container runtime could not be queried{% set _err = (h.audit_containers | default({})).stderr | default('') %}{% if _err %} (`{{ _err | trim | truncate(120) }}`){% endif %}. This is not the same as "no containers" — treat it as unknown.
{% elif not h.audit_containers.stdout | trim %}
None.
{% else %}
{# NetworkMode names a peer by container ID, and an ID is not a name and not stable —
   it changes on every recreate. So build the host's own id -> name map first and render
   the name, or this file gains a meaningless one-line diff every time gluetun is
   recreated, which port-forwarding.md says is routine. #}
{% set id2name = {} %}
{% for line in h.audit_containers.stdout_lines %}
{% set g = line.split('|') %}
{% set _ = id2name.update({(g[7] | default('') | trim): g[0]}) %}
{% endfor %}
| Container | Image | State | Restart | Compose project | Published |
|---|---|---|---|---|---|
{% for line in h.audit_containers.stdout_lines %}
{% set f = line.split('|') %}
{# Published is the one cell that cannot be read straight off Docker. A container that
   does not own its network stack has NO port mappings, so the raw answer is empty for
   `syncthing` (host networking) and for `torrent` (inside gluetun's namespace) — and an
   empty cell there says "publishes nothing", which is false for both. NetworkMode is
   what tells the two apart, so the cell answers "how is this reached" instead. #}
{% set net = f[5] | default('') | trim %}
{% set pub = f[6] | default('') | trim %}
{% set peer = net.split(':', 1)[1] if ':' in net else '' %}
{# Falls back to the truncated id when the peer is not in this host's list — a namespace
   can be shared with a container that has since been removed, and saying so honestly
   beats rendering a blank. `service:<name>` from Compose passes through unchanged. #}
{% set reach = pub if pub
   else ('host net — see Listening' if net == 'host'
   else (('net of `' ~ id2name.get(peer, peer[:12]) ~ '`') if peer
   else '—')) %}
| `{{ f[0] }}` | {{ f[1] | default('') }} | {{ f[2] | default('') }} | {{ f[3] | default('') or '—' }} | {{ f[4] | default('') or '—' }} | {{ reach }} |
{% endfor %}
{% endif %}

### Listening

{% if h.audit_sockets is not defined or h.audit_sockets.rc | default(1) != 0 %}
**UNREADABLE** — treat as unknown, not as nothing listening.
{% elif not h.audit_sockets.stdout | trim %}
Nothing.
{% else %}
```
{{ h.audit_sockets.stdout | trim }}
```
{% endif %}

### Volumes

{% if host not in groups['containerized'] | default([]) %}
Not audited.
{% elif h.audit_volumes is not defined or h.audit_volumes.rc | default(1) != 0 %}
**UNREADABLE.**
{% elif not h.audit_volumes.stdout | trim %}
None.
{% else %}
{% for v in h.audit_volumes.stdout_lines %}
- `{{ v }}`
{% endfor %}
{% endif %}

{% endfor %}
---

**Reading this file.** `UNREADABLE` and `None` mean different things and are never
collapsed: a failed read looks exactly like an empty result, and treating one as the other
is how a check passes for the wrong reason. A host that answered and holds nothing says
`None`; a host that could not be asked says `UNREADABLE`.

**Published is "how is this reached", not "what did Docker map".** A container on host
networking or inside another container's namespace has no port mappings at all, so the
literal answer for both Syncthings and for `torrent` is empty — and empty would read as
"publishes nothing" while `zero`'s Syncthing serves 8384/22000 and `torrent` answers on
8080. Those say `host net` and `net of <container>` instead, and the ports themselves are
in **Listening**, which reads the kernel and so covers every case. A bare `—` here means
Docker maps nothing *and* the container owns its own stack. The peer is shown by **name**,
not by the container id Docker actually reports — an id changes on every recreate, and
recreating gluetun is routine here, so the id would put a meaningless diff in this file
each time.

**Listening is the authority when the two disagree.** Containers is what Docker was
configured to do; Listening is what the box is doing. `one`'s Syncthing is the worked
example: it is defined with host networking on 8384/22000, it is `exited`, and neither
port appears below it.
