# `two` — hardware

Generated by `bin/hw-inventory`. Do not hand-edit: corrections belong in
`hosts/two/system/hw-inventory.toml` and are merged in on the next run.

## Machine

| | |
|---|---|
| Board | Raspberry Pi Model B Plus Rev 1.2 |
| CPU | ARMv6-compatible processor rev 7 (v6l) (1 cores) |
| Memory | 498M |
| Arch | armv6l |
| OS | Alpine Linux v3.24 |
| Kernel | 6.18.35-0-rpi |

## Drives

Indexed by what is legible on the drive itself. `?` means the fact is not
readable here — fill it in via the overrides file rather than guessing.

| Index | Dev | Bay | Role | Model | Serial |
|---|---|---|---|---|---|
| `32G-?-SD-?` | mmcblk0 | — | — | SL32G | 0x4def22b2 |

> **SMART was not read for this inventory.** Model, serial, form factor and
> manufacture date are only available through SMART for enclosure-attached
> disks — sysfs reports the bridge, not the drive. Re-run as root to fill
> these in.

> **No `hw-inventory.toml` for this host**, so SMART was skipped entirely and
> no overrides were applied. Create it to enable polling — listing the
> devices that must never be polled.

## USB topology

```
/:  Bus 001.Port 001: Dev 001, Class=root_hub, Driver=dwc_otg/1p, 480M
    |__ Port 001: Dev 002, If 0, Class=[unknown], Driver=hub/5p, 480M
        |__ Port 001: Dev 003, If 0, Class=[unknown], Driver=smsc95xx, 480M
```
